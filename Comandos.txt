Para correr los tests con 'npm run test'
se debe modificar el archivo package.json en la linea que dice scripts y dejarlo asi:
Suponiendo que el test que queremos ejecutar con este comando se llama TestElements.spec.js

"scripts": {
    "testElements": "npx playwright test TestElements.spec.js"
  },

Luego correr el comando asi: npm run testElements


Comandos git:
Para inicializar > git init
Para agregar > git add .
Para commitear > git commit -m "Comentario del commit"
Para ver el estado > git status
Para ver el historico de lo que commiteamos > git log
Para recuperar archivos borrados > git checkout -- .

subir codigo a GitHub
git remote add origin https://github.com/adgaleano/Curso_V1.git
git branch -M main
git push -u origin main 
(Autorizar)